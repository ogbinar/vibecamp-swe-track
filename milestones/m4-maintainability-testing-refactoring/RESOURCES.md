# Resource guide

Select primary FastAPI dependency documentation, pytest fixture/testing references, Python module/import guidance, and respected refactoring material centered on behavior preservation. Find modular-monolith material that defines ownership and dependency direction without requiring service extraction.

Reject pattern catalogs disconnected from a change, universal repository/base-service recipes, and mock-everything testing advice. Every resource must answer a pressure observed in C1–C3.
